# 当前运行设置

项目：记忆方舟
更新时间：2026-05-22

| 字段 | 当前选择 | 可选项 | 是否需要用户确认 |
|---|---|---|---|
| RUN_INTENT | 即梦网页端人工上传准备；当前仅整理 `S01E01_SEG001` | 规划/审查、单段小样、最终交付包、视频提交/下载 | CLI权限阻塞后改为手动网页端 |
| SOURCE_AUTHORITY | 原创生成 + 当前锁定资产优先 | 用户提供素材优先、当前锁定资产优先、原创生成 | 已确认 |
| ACTIVE_SCOPE | 第一季 EP01 三分钟样片全片；当前门禁集中在S01E01_SEG001 | 当前段、相邻两段、整集、整季 | 已确认 |
| EPISODE_SPEC | 3分钟样片；16:9横屏；16+悬疑；电影写实漫画；样片链路 | 90秒概念、3分钟样片、5-7分钟单集、10分钟+正片 | 已确认 |
| QUALITY_STAGE | 参考图已锁定；SEG001关键帧已用户确认；SEG001上传包viewer已用户确认；SEG001网页端人工上传包已整理 | 草稿、小样、连续性测试、最终包、批量 | 当前阶段已更新 |
| PROVIDERS | 图片/关键帧：`$imagegen_builtin_image_gen`；音频：Mimo 或 manifest_only；视频：`dreamina_cli_seedance2` | 固定正式路线 | 图片路线已确认；视频提交前再确认 |
| COST_POLICY | 本轮不再使用CLI；只整理网页端手动上传包；网页端生成由用户自行确认和提交 | 不消耗额度、每次提交前确认、本轮额度上限 | 当前不自动消耗额度 |
| CONTINUITY_STRATEGY | `keyframe_multiref_seedance`；SEG001无上一段尾帧，后续段SHOT01必须来自真实尾帧 | keyframe_multiref_seedance | 已确认 |
| LOCKED_ASSETS | 12张参考图小样锁定；SEG001四张关键帧待用户确认 | 使用当前锁定清单、允许新版本不覆盖、等待用户指定 | 当前不覆盖锁定资产 |
| OUTPUT_CONTRACT | 文本制作包、完整对白版、分镜表、视觉资产任务表、关键帧队列、SEG001正式上传包、上传包viewer确认记录、SEG001即梦网页端人工上传包 | 文件夹、上传包、视频路径、报告、zip | 已更新 |
| VERIFICATION | 规格检查、12段检查、每段4镜头检查、provider检查、关键帧存在性检查、无视频提交检查 | JSON/脚本检查、视觉人工检查、用户验收 | 已执行部分自检 |

## 已确认

- 片名：`记忆方舟`。
- 题材：末世生存 / 记忆饥荒 / 方舟骗局。
- 主角：陆沉，32岁失忆士兵。
- 关键配角：小禾、罗医师。
- 单集时长：约3分钟，12个15秒视频单元。
- 每个15秒单元固定4个SHOT，默认时间码为0-4秒、4-8秒、8-12秒、12-15秒。
- 视觉风格：电影感写实漫画，低饱和废墟、冷白医疗光、脏橙色应急灯、强阴影、写实镜头语言。
- 内容边界：16+悬疑，避免露骨血腥、字幕、UI、水印、二维码和可读文字。
- 参考图：12张已生成并由用户确认，资产清单登记为小样锁定。

## 当前待确认

1. 用户是否在即梦网页端使用 `jimeng_web_upload_packages/S01E01_SEG001_manual_web/` 生成视频。
2. 网页端生成后，用户需把下载结果放回 `manual_web_outputs/S01E01_SEG001/`。
3. 放回视频后再做质检、命名、尾帧提取和SEG002衔接。
4. 是否进入SEG002-SEG004所需参考资产补图和后续33张关键帧生成。
5. Mimo角色音色仍未生成；SEG001网页端包按无上传音频视频小样准备。

## 本轮不得做

- 不调用 `$imagegen` 实际生成新图片。
- 不调用 Mimo 生成音频。
- 不调用 `dreamina` CLI。
- 不代替用户在网页端点击生成。
- 不伪造后续段的上一段真实尾帧。
- 不自动把 `ready_for_video_submit` 等同于已授权消耗视频额度。

